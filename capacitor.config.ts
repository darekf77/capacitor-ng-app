import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-ng-app',
  webDir: 'dist/capacitor-ng-app/browser'
};

export default config;
